public class Steps {
    public static void main(String[] args) {
        int[][] grid ={
            {1,2,3},
            {4,5,6},
            {7,8,9}
        };
        int n = grid.length;
        int m = grid[0].length;
        System.out.println("Max Number of steps to reach the end: " + countSteps(n, m, grid));
    }
    static int countSteps(int n, int m, int[][] grid) {
        int result = 0;

        return result;
    }
}
